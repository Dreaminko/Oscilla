<Window x:Class="Oscilla.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Oscilla.UI"
        Title="Oscilla" Icon="pack://application:,,,/source/icon.ico" Height="720" Width="1280"
        Background="#000000" WindowStyle="None" AllowsTransparency="True" 
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <LinearGradientBrush x:Key="AuroraTextBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#00FFAB" Offset="0"/>
            <GradientStop Color="#00D1FF" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="SystemBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2E2E2E"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="Foreground" Value="#D1D1D1"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="75"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0A0A0A" MouseLeftButtonDown="TitleBar_MouseDown">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,0,0,0">
                    <TextBlock Text="oscilla" Foreground="{StaticResource AuroraTextBrush}" FontSize="18" FontWeight="Bold" />
                    <TextBlock Text="powered by Naudio" Foreground="#555555" FontSize="10" Margin="8,0,0,2" VerticalAlignment="Bottom" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="—" Click="Minimize_Click" Style="{StaticResource SystemBtnStyle}"/>
                    <Button Content="✕" Click="Close_Click" Style="{StaticResource SystemBtnStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Name="MainWorkspace" Background="#000000">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:NavigationSidebar Grid.Column="0" x:Name="Sidebar" />
            <Rectangle Grid.Column="1" Fill="#1E1E1E" Width="1" VerticalAlignment="Stretch" Margin="0,30"/>

            <ContentControl Grid.Column="2" Name="MainContentArea" Margin="20">
                <Grid>
                    <TextBlock Text="oscilla" Foreground="{StaticResource AuroraTextBrush}" Opacity="0.04" FontSize="220" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </ContentControl>
        </Grid>

        <local:PlayerBar Grid.Row="2" x:Name="BottomBar"/>

        <Grid x:Name="DeleteOverlay" Grid.RowSpan="3" Visibility="Collapsed" Background="#B2000000" Panel.ZIndex="999">
            <Border Background="#111111" BorderBrush="#2E2E2E" BorderThickness="1" CornerRadius="12" Width="280" Height="140" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock x:Name="DeleteTargetText" Text="Delete playlist?" Foreground="#FFFFFF" FontSize="15" FontWeight="Medium" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Cancel" Click="CancelDelete_Click" Style="{StaticResource DialogBtnStyle}" Margin="0,0,15,0"/>
                        <Button Content="Delete" Click="ConfirmDelete_Click" Style="{StaticResource DialogBtnStyle}" Foreground="#FF4A4A"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <local:Start x:Name="StartOverlay" Grid.RowSpan="3" Panel.ZIndex="1000" />

    </Grid>
</Window>